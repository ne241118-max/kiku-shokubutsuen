# 聞く植物園 QR音声ガイド

GitHub Pagesで公開するためのファイルです。

## 使い方

1. GitHubで `kiku-shokubutsuen` というリポジトリを作る
2. この中の `index.html` をアップロードする
3. Settings → Pages
4. Source を「Deploy from a branch」にする
5. Branch を `main`、フォルダを `/(root)` にして Save
6. 表示されたURLをQRコード作成サイトに入れる

